name = "investhelper-yahoo-proxy"
main = "worker.js"
compatibility_date = "2025-01-01"

# Optionnel : si tu mets une valeur ici (via secrets, pas en clair), le Worker
# exigera un header `Authorization: Bearer <valeur>` sur chaque requête.
# Recommandé pour ne pas exposer ton Worker à tous les bots du Net.
#
# Pour la définir :
#   wrangler secret put PROXY_SECRET
#
# [vars]
# (aucune var en clair)
