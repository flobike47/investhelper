# BFF — endpoint des données (providers + DB user)
VITE_BFF_URL=http://localhost:4000

# Supabase — auth Google (clés publiques, safe en client)
# Récupérables sur supabase.com → Project Settings → API
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
